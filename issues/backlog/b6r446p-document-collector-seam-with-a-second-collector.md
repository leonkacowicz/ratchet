# Document Collector seam with a second collector

## Summary
Add a second `Collector` (or a documented recipe) so that adding new metric sources is a
well-trodden path rather than a one-off. The trait already exists for this; `structural`
is currently the only impl.

## Acceptance criteria
- [ ] A second collector or a documented extension recipe exists
